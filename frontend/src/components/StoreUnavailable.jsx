import { ShoppingBag } from 'lucide-react';

const StoreUnavailable = () => (
  <section className="store-unavailable" dir="rtl">
    <div>
      <ShoppingBag size={34} />
    </div>
    <h2>المتجر غير متاح حالياً</h2>
  </section>
);

export default StoreUnavailable;
